export const environment = {
     apiUrl: 'https://amedic-backend.onrender.com'
    // apiUrl: 'http://localhost:3000'
};

// the backend route used during development, change it accordingly during hosting
